-- init.sql
CREATE DATABASE IF NOT EXISTS money_transfer_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE money_transfer_db;

-- يمكنك إضافة بيانات أولية هنا
-- INSERT INTO users (username, email, password, role, points) VALUES ('admin', 'admin@example.com', '$2b$12$...', 'admin', 10000);